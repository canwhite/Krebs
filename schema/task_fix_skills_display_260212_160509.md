# Task: 彻底修复 UI Skills 显示问题

**任务ID**: task_fix_skills_display_260212_160509
**创建时间**: 2026-02-12
**状态**: 已修复，待用户验证
**目标**: 彻底解决 UI 只显示 7 个 skills 而不是 8 个的问题

## 最终目标
UI 能正确显示 skills/bundled 目录中的所有 8 个 skills

## 当前进度
### 已完成：彻底修复

**根本原因**：`test-add-skill/SKILL.md` 文件格式有多个问题

**问题 1**：description 字段没有用引号包围
```yaml
description: [TODO: ...]  # 错误：方括号导致解析为数组
```

**问题 2**：metadata 中的 JSON 格式错误
```yaml
metadata: '{"krebs":{{...}}}'  # 错误：嵌套花括号
```

**已修复**：
```yaml
name: test-add-skill
description: "测试技能：用于验证技能系统是否正常工作"
metadata: '{"krebs":{"emoji":"🔧","category":"Test","tags":["test","example"]}}'
```

**验证结果**：
```bash
# 本地加载测试：8 个 skills ✅
# API 返回测试：8 个 skills ✅
# test-add-skill 存在：✅
```

## 用户操作步骤

由于代码已重新构建（`npm run build`），但服务器进程可能还在使用旧代码：

```bash
# 方法 1：完全重启（推荐）
# 1. 停止所有 krebs 进程
pkill -f "tsx.*src/index.ts" || true
pkill -f "node.*krebs" || true

# 2. 重新启动
npm start

# 方法 2：如果在开发模式
# 1. 按 Ctrl+C 停止当前 watch 进程
# 2. 重新运行
npm run watch  # 或 npm start
```

重启后，刷新浏览器应该能看到 8 个 skills。
