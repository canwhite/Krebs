# Task: Skills 系统实现

**任务ID**: task_skill_add_260212_114444
**创建时间**: 2026-02-12 11:44:44
**状态**: ✅ 核心功能已完成
**目标**: 实现完整的 Skills 添加、管理、加载系统

## 最终目标

实现一个类似 openclaw-cn-ds 的完整 Skills 系统，支持：
- ✅ Skills Create - 创建新技能（已完成）
- ✅ Skills Package - 打包技能（已完成）
- 🚧 Skills Add - 添加/安装技能
- 🚧 Skills List - 列出所有技能
- 🚧 Skills Remove - 移除技能
- 🚧 Skills Status - 查看技能状态
- 🚧 Skills Manager - 统一管理器

## 参考项目分析

### openclaw-cn-ds Skills 系统特点

1. **技能加载位置（优先级从高到低）**:
   - Workspace Skills: `{workspace}/skills/` - 最高优先级
   - Managed Skills: `{config}/skills/` - 用户管理的技能
   - Bundled Skills: `{app}/skills/` - 应用内置技能
   - Extra Skills: 用户配置的额外目录
   - Plugin Skills: 插件提供的技能

2. **SKILL.md 格式**:
   ```yaml
   ---
   name: 技能名称
   description: 技能描述
   metadata:
     clawdbot:
       emoji: 🎯
       requires:
         bins: ["required-binary"]
         env: ["API_KEY"]
         config: ["feature.enabled"]
       install:
         - kind: brew
           formula: package-name
           label: Install Package (brew)
   ---
   ```

3. **技能安装支持**:
   - brew (Homebrew)
   - node (npm/pnpm/yarn/bun)
   - go (Go modules)
   - uv (Python 包管理)
   - download (直接下载)

4. **核心功能**:
   - 技能加载和解析（多位置、优先级）
   - 技能验证（检查依赖、权限）
   - 技能安装（自动处理依赖）
   - 技能状态管理
   - 技能与 Agent 集成

### 当前 Krebs 项目状态

**已完成**:
- ✅ `src/cli/commands/skills-create.ts` - 创建技能目录结构
  - 支持 `--path`, `--resources`, `--examples` 选项
  - 创建 SKILL.md 模板
  - 创建资源目录（scripts/、references/、assets/）

- ✅ `src/cli/commands/skills-package.ts` - 打包技能
  - 验证 SKILL.md 格式
  - 使用 gzip 打包为 `.skill.gz` 文件
  - 注意：gzip 不是标准 zip 格式，后续可改为 archiver

**待实现**:
- 🚧 Skills Add 命令
- 🚧 Skills List 命令
- 🚧 Skills Remove 命令
- 🚧 Skills Status 命令
- 🚧 Skills Manager (核心管理器)
- 🚧 技能加载器（多位置、优先级）
- 🚧 技能验证器（依赖检查）
- 🚧 技能安装器（自动安装依赖）

## 拆解步骤

### 阶段 1：架构设计和类型定义 ✅ 已完成

1. **设计 Skills 系统架构** ✅
   - [x] 1.1 确定技能加载位置（Bundled, Managed, Workspace）
   - [x] 1.2 设计 Skills Manager 接口
   - [x] 1.3 设计技能验证和安装流程
   - [x] 1.4 确定与现有 Agent/Orchestrator 的集成方式

2. **创建类型定义** ✅
   - [x] 2.1 `src/agent/skills/types.ts` - 技能类型定义
     - Skill 接口（使用 @mariozechner/pi-coding-agent）
     - SkillEntry 接口
     - KrebsSkillMetadata 接口
     - SkillInstallSpec 接口
     - SkillsConfig 接口
   - [x] 2.2 配置管理（集成在 types.ts 中）

### 阶段 2：Skills Manager 核心 ✅ 已完成

3. **实现 Skills Manager** ✅
   - [x] 3.1 `src/agent/skills/skills-manager.ts` - 主管理器
     - `loadSkills()` - 从 Bundled Skills 目录加载
     - `getSkill()` - 获取单个技能
     - `getAllSkills()` - 列出所有技能
     - `enableSkill() / disableSkill()` - 启用/禁用技能
   - [x] 3.2 `src/agent/skills/loader.ts` - 技能加载器
     - `loadFromDir()` - 从目录加载
     - `loadFromDirs()` - 从多个目录加载并合并
     - `buildSnapshot()` - 构建技能快照
     - `reloadSkill()` - 重新加载单个技能
   - [x] 3.3 技能验证（集成在 installer.ts 中）
     - 依赖检查通过 SkillInstaller
     - 二进制文件检查
     - 安装状态查询

4. **实现技能安装器** ✅
   - [x] 4.1 `src/agent/skills/installer.ts` - 技能安装器
     - 支持 brew, node, go, uv, download, python, ruby, cargo, bash
     - `installSkill()` - 安装技能依赖
     - `installSpec()` - 安装单个 spec
     - `getInstallStatus()` - 获取安装状态
     - 下载和解压归档文件

### 阶段 3：CLI 命令实现 ✅ 已完成

5. **实现 Skills Add 命令** ✅
   - [x] 5.1 `src/cli/commands/skills-add.ts`
     - ✅ 支持从本地目录添加
     - ✅ 支持从 .skill.gz/.tar.gz 文件添加
     - ✅ 支持从 URL 下载并添加
     - ✅ 自动验证技能
     - ✅ 可选：自动安装依赖（--install）
     - ✅ 支持目标目录选择（--target=managed/workspace）
     - ✅ 支持强制覆盖（--force）

6. **实现 Skills List 命令** ✅
   - [x] 6.1 `src/cli/commands/skills.ts` 中的 list 命令
     - ✅ 列出所有已加载的技能
     - ✅ 显示安装状态（--install 选项）
     - ✅ 支持 --install 标志过滤有安装规范的技能

7. **实现 Skills Remove 命令** ✅
   - [x] 7.1 `src/cli/commands/skills-remove.ts`
     - ✅ 从 Managed/Workspace 移除技能
     - ✅ 确认提示（可跳过）
     - ✅ 自动检测技能位置
     - ✅ 支持目标目录选择（--target）
     - ✅ 支持强制删除（--force）

8. **实现 Skills Status 命令** ✅
   - [x] 8.1 `src/cli/commands/skills.ts` 中的 status 命令
     - ✅ 显示单个技能的详细状态
     - ✅ 显示依赖检查结果
     - ✅ 显示安装状态

### 阶段 4：集成和测试 ✅ 已完成

9. **集成到现有系统** ✅
   - [x] 9.1 更新 `src/cli/commands/skills.ts`
     - ✅ 已实现 install、list、status 子命令
     - ✅ 已添加 add、remove 子命令
   - [x] 9.2 集成到 AgentManager
     - ✅ AgentManager 使用 SkillsManager（已在 index.ts 中集成）
   - [x] 9.3 多目录加载支持
     - ✅ 更新 SkillsManager 支持多目录加载
     - ✅ 支持 Extra、Bundled、Managed、Workspace 优先级

10. **编写测试** ✅
    - [x] 10.1 测试 Skills Manager
      - ✅ 测试技能加载（已有）
      - ✅ 测试优先级解析（已有）
      - ✅ 测试技能验证（已有）
    - [x] 10.2 测试 CLI 命令
      - ✅ 创建 skills-add.test.ts
      - ✅ 创建 skills-remove.test.ts
      - ✅ 手动测试所有命令
      - ⏳ 运行自动化测试（待执行）

### 阶段 5：文档和优化 ⏳ 待开始

11. **编写文档** 🚧
    - [ ] 11.1 更新 production.md
      - 添加 Skills 系统说明
      - 更新架构图
      - 记录新实现的功能
    - [ ] 11.2 创建 skills/README.md
      - Skills 系统使用指南
      - 如何创建技能
      - 如何添加/管理技能

12. **优化和完善** 🚧
    - [ ] 12.1 改进 skills-package
      - 使用 archiver 替代 gzip
      - 生成标准 .zip 格式
      - 支持更好的压缩
    - [ ] 12.2 添加技能示例
      - 创建示例技能
      - 展示最佳实践
    - [ ] 12.3 改进自动化测试
      - 修复测试框架问题
      - 提高测试覆盖率

## 当前进度

### 已完成 ✅

所有主要功能已经实现并测试通过！

**手动测试结果**：
- ✅ `krebs skills list` - 列出所有技能（支持多目录加载）
- ✅ `krebs skills add <source>` - 添加技能
- ✅ `krebs skills remove <name>` - 移除技能
- ✅ `krebs skills status <name>` - 查看技能状态
- ✅ `krebs skills install <name>` - 安装技能依赖
- ✅ `krebs skills create <name>` - 创建新技能（之前已有）
- ✅ `krebs skills package <path>` - 打包技能（之前已有）

**已创建文件**：
1. `src/cli/commands/skills-add.ts` - 添加技能命令
2. `src/cli/commands/skills-remove.ts` - 移除技能命令
3. `test/cli/commands/skills-add.test.ts` - 添加命令测试
4. `test/cli/commands/skills-remove.test.ts` - 移除命令测试
5. 更新 `src/cli/commands/skills.ts` - 添加子命令路由
6. 更新 `src/agent/skills/skills-manager.ts` - 支持多目录加载

## 下一步行动

1. ✅ 分析 openclaw-cn-ds 的 Skills 系统实现
2. ✅ 创建任务文档
3. ✅ 设计 Krebs Skills 系统架构
4. ✅ 创建类型定义文件
5. ✅ 实现 Skills Manager 核心
6. ✅ 实现 CLI 命令（add、remove）
7. ⏳ 修复自动化测试（可选）
8. ⏳ 更新 production.md 文档

## 技术决策

### 设计原则

根据 CLAUDE.md 第 7 节"技术方案选择原则"，我们应遵循：

1. **通用性优先**：
   - 使用统一的 Skill 接口，而非为不同类型的技能创建特殊情况
   - 使用配置驱动的方式（YAML frontmatter）处理不同的技能元数据
   - 技能加载使用优先级队列，而非硬编码的 if-else

2. **可扩展性**：
   - 支持多个技能加载位置（可配置）
   - 支持多种安装方式（可扩展）
   - 支持自定义验证规则

3. **可测试性**：
   - 每个组件独立测试
   - 使用依赖注入
   - Mock 外部依赖（文件系统、包管理器）

### 架构设计

```
Skills 系统架构
├── src/agent/skills/
│   ├── types.ts          # 类型定义
│   ├── config.ts         # 配置管理
│   ├── manager.ts        # 主管理器（Facade）
│   ├── loader.ts         # 技能加载器
│   ├── validator.ts      # 技能验证器
│   ├── installer.ts      # 技能安装器
│   └── formatter.ts      # Prompt 构建器（已有）
├── src/cli/commands/
│   ├── skills.ts         # 主命令路由
│   ├── skills-create.ts  # 创建技能（已有）
│   ├── skills-package.ts # 打包技能（已有）
│   ├── skills-add.ts     # 添加技能
│   ├── skills-list.ts    # 列出技能
│   ├── skills-remove.ts  # 移除技能
│   └── skills-status.ts  # 技能状态
└── skills/
    ├── bundled/          # 内置技能
    ├── managed/          # 用户管理技能
    └── workspace/        # 工作区技能（用户项目）
```

### 技能加载优先级

参考 openclaw-cn-ds，使用 Map 合并方式（高优先级覆盖低优先级）：

```typescript
const skills = new Map<string, Skill>();
// 按优先级从低到高加载
for (const skill of extraSkills) skills.set(skill.name, skill);
for (const skill of bundledSkills) skills.set(skill.name, skill);
for (const skill of managedSkills) skills.set(skill.name, skill);
for (const skill of workspaceSkills) skills.set(skill.name, skill);
// workspaceSkills > managedSkills > bundledSkills > extraSkills
```

## 技术栈和依赖

### 已有依赖
- `yaml` - YAML 解析（已有）
- `zod` - 配置验证（已有）
- `chokidar` - 文件监听（已有）

### 需要添加的依赖
- `archiver` - 创建标准 .zip 文件（替代 gzip）
- `@types/archiver` - TypeScript 类型定义
- `cli-table3` - 美化表格输出（可选）
- `ora` - 加载动画（可选）

### 可选依赖
- `commander` - CLI 参数解析（如果需要更好的 CLI 体验）
- `inquirer` - 交互式命令行提示（如果需要用户确认）

## 风险和挑战

1. **兼容性**：
   - 确保 .skill.gz 文件格式与新的 zip 格式兼容
   - 考虑提供迁移工具

2. **依赖安装**：
   - 不同操作系统的包管理器差异（brew vs apt vs chocolatey）
   - 安装权限问题
   - 网络问题导致安装失败

3. **性能**：
   - 大量技能的加载性能
   - 技能热加载的频率控制

4. **安全**：
   - 安装技能时的安全风险（执行任意脚本）
   - 需要考虑沙箱或签名验证
