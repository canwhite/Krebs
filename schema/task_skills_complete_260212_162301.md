# Task: Skills 系统完整实现

**任务ID**: task_skills_complete_260212_162301
**创建时间**: 2026-02-12
**状态**: 已完成
**目标**: 完整实现 skills 验证和热加载功能

## 最终成果

### ✅ 1. 热加载功能
**文件**: `src/index.ts`

修改 skill 文件后自动重新加载，**无需手动重启服务器**

**使用方法**：
1. 在 `skills/bundled/` 创建新的 skill 目录和 SKILL.md
2. 保存文件
3. 等待 1-2 秒
4. 刷新浏览器，新的 skill 自动出现

### ✅ 2. 格式验证
**文件**: `src/agent/skills/validator.ts`（新建）

**验证规则**：
- ✅ name 必须是小写字母、数字、连字符
- ✅ description 必须是字符串，包含特殊字符时用引号
- ✅ metadata 必须是有效的 JSON 字符串
- ✅ 必须包含 name 和 description 字段

**工作模式**：
- **默认（警告模式）**：格式问题只记录警告，仍然加载
- **严格模式**：设置 `SKILLS_VALIDATOR_STRICT=true` 环境变量时拒绝

### ✅ 3. CLI 验证命令
**文件**: `src/cli/commands/skills-validate.ts`（新建）

**使用方法**：
```bash
# 验证默认目录
npm run validate:skills

# 验证自定义目录
krebs validate:skills skills/custom
```

## Skill 格式规范

根据 pi-coding-agent 的标准，**name 字段应该是小写**。

### 正确示例

```yaml
---
name: my-skill
description: "技能描述"
metadata: '{"krebs":{"emoji":"🔧","category":"Test","tags":["test"]}}'
---

# My Skill

技能内容...
```

### 常见错误

| 错误 | 正确方式 | 说明 |
|--------|----------|------|
| `name: MySkill` | `name: my-skill` | name 必须小写 |
| `description: [TODO: ...]` | `description: "TODO: ..."` | 特殊字符用引号 |
| `metadata: {...}` | `metadata: '{...}'` | metadata 是 JSON 字符串 |

### 关于现有 Skills

项目中的现有 skills（ExampleBashInstall、Filesystem 等）使用大写名称是**历史遗留问题**，不符合规范。

为了兼容，验证器设置为**警告模式**（默认），所以这些 skills 仍然可以加载。如果需要符合规范，应该：

1. 重命名目录为小写（如 `filesystem`）
2. 更新 SKILL.md 中的 name

## 验证测试结果

✅ **测试已创建**：`skills/bundled/test-skill/SKILL.md`
✅ **格式完全正确**：name 小写，description 用引号
✅ **热加载生效**：保存文件后立即出现在 API
✅ **API 返回 8 个 skills**：

1. ExampleBashInstall
2. Filesystem
3. GitHub
4. Notion
5. Summarize
6. Tmux
7. WebSearch
8. **test-skill**（新创建）

## 使用指南

### 添加新 Skill

```bash
# 1. 创建目录
mkdir -p skills/bundled/my-new-skill

# 2. 创建 SKILL.md
cat > skills/bundled/my-new-skill/SKILL.md << 'EOF'
---
name: my-new-skill
description: "我的新技能"
metadata: '{"krebs":{"emoji":"⚡","category":"General","tags":["new"]}}'
---

# My New Skill

技能功能描述...
EOF

# 3. 保存文件，热加载会自动处理
# 4. 刷新浏览器查看
```

### 验证 Skills

```bash
npm run validate:skills
```

### 严格模式（可选）

如果只想加载符合规范的 skills：

```bash
SKILLS_VALIDATOR_STRICT=true npm start
```

## 完整功能列表

1. ✅ **热加载**：文件修改自动生效
2. ✅ **格式验证**：警告格式问题（默认）
3. ✅ **CLI 验证**：批量验证所有 skills
4. ✅ **错误提示**：清晰的日志输出
5. ✅ **兼容性**：现有 skills 继续工作
