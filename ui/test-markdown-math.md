# Markdown 数学公式测试

## 行内公式测试

这是简单的行内公式：$E = mc^2$

这是复杂的行内公式：$\text{softmax}(Q\tilde{K}^T) \approx \text{softmax}(QK^T)$

分数测试：$\frac{a}{b} + \frac{c}{d}$

上下标测试：$x_1^2 + y_2^2 = z_3^2$

## 块级公式测试

简单的勾股定理：
$$
a^2 + b^2 = c^2
$$

二次方程求根公式：
$$
x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
$$

矩阵示例：
$$
\begin{pmatrix}
a & b \\
c & d
\end{pmatrix}
\times
\begin{pmatrix}
e & f \\
g & h
\end{pmatrix}
=
\begin{pmatrix}
ae + bg & af + bh \\
ce + dg & cf + dh
\end{pmatrix}
$$

积分示例：
$$
\int_0^\infty e^{-x^2} dx = \frac{\sqrt{\pi}}{2}
$$

求和示例：
$$
\sum_{i=1}^{n} i = \frac{n(n+1)}{2}
$$

用户提到的论文公式：
$$
\text{softmax}(Q\tilde{K}^T) \approx \text{softmax}(QK^T)
$$

## 复杂组合公式

极限：
$$
\lim_{x \to \infty} \left(1 + \frac{1}{x}\right)^x = e
$$

希腊字母：
$$
\alpha, \beta, \gamma, \delta, \epsilon, \theta, \lambda, \mu, \pi, \sigma, \omega, \Omega
$$

括号组合：
$$
\left( \sum_{i=1}^{n} a_i \right) \times \left( \prod_{j=1}^{m} b_j \right)
$$
