# Krebs Skills 系统完整分析

> 本文档详细分析 Krebs 项目中 Skills 系统的设计与实现

## 目录

- [整体架构](#1-整体架构)
- [技能加载流程](#2-技能加载流程)
- [技能执行流程](#3-技能执行流程)
- [核心代码展示](#4-核心代码展示)
- [技能依赖安装](#5-技能依赖安装流程)
- [热重载机制](#6-热重载流程)
- [关键特性](#7-关键特性总结)
- [双系统设计](#8-双系统设计)

---

## 1. 整体架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              用户交互层                                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                   │
│  │ CLI Commands │  │   UI Panel   │  │  HTTP API    │                   │
│  │ /skills      │  │ krebs-skills-│  │  /api/skills │                   │
│  │              │  │  panel.ts    │  │              │                   │
│  └──────────────┘  └──────────────┘  └──────────────┘                   │
└─────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         SkillsManager (Facade)                          │
│  src/agent/skills/skills-manager.ts                                     │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  核心职责:                                                       │   │
│  │  - loadSkills()        加载所有技能                              │   │
│  │  - buildSkillsPrompt() 构建提示词                                │   │
│  │  - installSkillDeps()  安装依赖                                  │   │
│  │  - enable/disableSkill() 启用/禁用技能                           │   │
│  │  - enableHotReload()   启用热重载                                │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                   │                                     │
│           ┌───────────────────────┼───────────────────────┐             │
│           ▼                       ▼                       ▼             │
│  ┌──────────────┐       ┌──────────────┐       ┌──────────────┐        │
│  │   Loader     │       │  Formatter   │       │  HotReload   │        │
│  │              │       │              │       │              │        │
│  └──────────────┘       └──────────────┘       └──────────────┘        │
└─────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         技能执行集成层                                    │
│  ┌──────────────────────────────┐  ┌──────────────────────────────┐   │
│  │  Orchestrator                │  │  System Prompt Builder       │   │
│  │  (orchestrator.ts)           │  │  (system-prompt.ts)          │   │
│  │                              │  │                              │   │
│  │  - process() 用户消息处理    │  │  - buildAgentSystemPrompt()  │   │
│  │  - 委托给 Agent 执行         │  │  - buildSkillsSection()      │   │
│  └──────────────────────────────┘  └──────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         技能存储层                                        │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │  /skills/bundled/              /skills/local/                    │  │
│  │  ├── github/SKILL.md          ├── (user skills)                 │  │
│  │  ├── filesystem/SKILL.md                                      │  │
│  │  ├── web-search/SKILL.md                                       │  │
│  │  └── ...                                                      │  │
│  └──────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         外部依赖层                                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                   │
│  │pi-coding-    │  │  chokidar    │  │ Package      │                   │
│  │  agent       │  │  文件监听    │  │ Managers     │                   │
│  │(技能加载)     │  │  (热重载)    │  │(npm/brew/..) │                   │
│  └──────────────┘  └──────────────┘  └──────────────┘                   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 技能加载流程

```
用户启动应用
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│  createDefaultSkillsManager()                               │
│  - 设置技能目录: /skills/bundled/                           │
│  - 创建 Loader, Formatter, HotReload 实例                   │
└─────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│  loadSkills() 调用                                          │
│  - Loader.loadFromDir("/skills/bundled/", "bundled")        │
└─────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│  pi-coding-agent 加载 SKILL.md 文件                         │
│  - 解析 frontmatter (name, description, metadata, install)  │
│  - 读取技能内容 (Markdown)                                  │
│  - 构建 SkillEntry[] 数组                                    │
└─────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│  buildSnapshot(entries, version)                            │
│  - 生成快照对象                                              │
│  - 包含所有技能、版本号、计数                                │
└─────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│  ✅ 技能加载完成                                             │
│  - 可通过 getSkills() 获取所有技能                           │
│  - 可通过 buildSkillsPrompt() 构建提示词                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 技能执行流程

**新系统 - System Prompt 方式（推荐）**

```
用户输入: "帮我创建一个 GitHub PR"
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│  Orchestrator.process(userMessage)                          │
│  src/agent/core/orchestrator.ts                             │
└─────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│  buildAgentSystemPrompt(config)                             │
│  src/agent/core/system-prompt.ts:75                         │
│  - 获取已启用技能列表                                        │
│  - 调用 buildSkillsSection(skills)                          │
└─────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│  buildSkillsSection(skills)                                 │
│  src/agent/core/system-prompt.ts:173                        │
│  - 格式化每个技能: name, description, prompt                │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ ## Skills (Available)                              │  │
│  │                                                      │  │
│  │ You have access to the following specialized skills:│  │
│  │                                                      │  │
│  │ - `github`: 使用 gh CLI 与 GitHub 交互              │  │
│  │   支持管理 issues、PRs、CI runs...                   │  │
│  │ - `filesystem`: 文件系统操作技能                     │  │
│  │   支持读取、写入、搜索文件...                         │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│  Agent.process(message, systemPrompt)                       │
│  - 将技能信息注入 System Prompt                             │
│  - LLM 看到可用技能列表                                     │
│  - LLM 自主判断何时使用哪个技能                             │
└─────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│  LLM 返回响应                                               │
│  - 可能建议: "使用 gh pr create 命令..."                     │
│  - 或: "我可以帮你用 GitHub skill 创建 PR..."               │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 核心代码展示

### 4.1 SkillsManager - 核心管理类

**文件位置**: `src/agent/skills/skills-manager.ts`

```typescript
export class SkillsManager {
  private loader: SkillsLoader;
  private formatter: SkillsFormatter;
  private hotReload: SkillsHotReload;
  private snapshot: SkillSnapshot | null = null;

  // 🔑 加载所有技能
  async loadSkills(): Promise<void> {
    const entries = this.loader.loadFromDir(
      this.config.bundledSkillsDir,
      "bundled"
    );

    this.version++;
    this.snapshot = this.loader.buildSnapshot(entries, this.version);
  }

  // 🎯 构建技能 Prompt (核心方法)
  buildSkillsPrompt(options?: BuildPromptOptions): string {
    if (!this.snapshot) return "";
    return this.formatter.formatForPrompt(this.snapshot.skills, options);
  }

  // ⚡ 启用/禁用技能
  enableSkill(name: string): boolean {
    const skill = this.getSkillByName(name);
    if (!skill) return false;
    skill.enabled = true;
    this.updateSnapshot();
    return true;
  }

  // 🔧 安装技能依赖
  async installSkillDeps(skillName: string): Promise<SkillInstallResult[]> {
    const skill = this.getSkillByName(skillName);
    const installer = getSkillInstaller();
    return await installer.installSkill(skill, options);
  }
}
```

### 4.2 System Prompt 集成

**文件位置**: `src/agent/core/system-prompt.ts`

```typescript
export function buildAgentSystemPrompt(config: SystemPromptConfig): string {
  const parts: string[] = [];

  // 基础身份
  parts.push(buildBaseSection(config.basePrompt));

  // 🎯 工具列表
  if (config.tools?.length > 0) {
    parts.push(buildToolsSection(config.tools));
  }

  // ⭐ 技能列表 (关键部分)
  if (config.skills?.length > 0) {
    parts.push(buildSkillsSection(config.skills));
  }

  return parts.join("\n\n");
}

// 构建技能部分 (line 173-191)
function buildSkillsSection(skills: Array<{
  name: string;
  description: string;
  prompt?: string
}>): string {
  const skillList = skills.map(skill => {
    let desc = `- \`${skill.name}\`: ${skill.description}`;
    if (skill.prompt) {
      desc += `\n  ${skill.prompt}`;
    }
    return desc;
  }).join("\n");

  return `## Skills (Available)

You have access to the following specialized skills:

${skillList}

These skills provide additional capabilities. Use them when relevant to the task.`;
}
```

### 4.3 技能文件结构

**文件位置**: `skills/bundled/github/SKILL.md`

```markdown
---
name: GitHub
description: "使用 `gh` 命令行工具与 GitHub 交互"
metadata: '{"krebs":{"emoji":"🐙","category":"Development","tags":["github","devops"]}}'
install:
  - kind: "brew"
    formula: "gh"
    bins: ["gh"]
---

# GitHub Skill

使用 `gh` CLI 工具与 GitHub 进行交互...

## Pull Requests

检查 PR 的 CI 状态：
\`\`\`bash
gh pr checks 55 --repo owner/repo
\`\`\`

## Issues

列出仓库的 issues：
\`\`\`bash
gh issue list --repo owner/repo --limit 20
\`\`\`
```

---

## 5. 技能依赖安装流程

```
用户执行: skills install github
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│  SkillsManager.installSkillDeps("github")                   │
│  - 获取技能对象                                             │
│  - 读取 frontmatter.install 字段                           │
└─────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│  解析 install 规范                                           │
│  支持 8 种安装类型:                                          │
│  - brew: Homebrew 包                                        │
│  - node: npm/pnpm/yarn 包                                   │
│  - go: go install                                           │
│  - uv: uv tool                                              │
│  - python: pip/pipx/poetry                                  │
│  - ruby: gem                                                │
│  - cargo: cargo install                                     │
│  - download: 下载并解压                                      │
└─────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│  执行安装命令                                                │
│  - 检测包管理器是否存在                                     │
│  - 执行安装命令                                             │
│  - 验证安装结果                                             │
└─────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│  返回安装结果                                                │
│  SkillInstallResult[]                                       │
│  - kind: 安装类型                                           │
│  - success: 是否成功                                        │
│  - message: 详细信息                                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 6. 热重载流程

```
开发人员修改 SKILL.md
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│  chokidar 监听文件变化                                       │
│  - .add("/skills/bundled")                                  │
│  - .on("change", handleFileChange)                          │
└─────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│  HotReload 检测到变化                                        │
│  - 防抖延迟 (避免频繁触发)                                   │
│  - 触发回调: onChange()                                      │
└─────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│  SkillsManager.handleReload()                               │
│  - 重新调用 loadSkills()                                    │
│  - 重新构建快照                                             │
│  - 版本号递增                                               │
└─────────────────────────────────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────────────────────────────────┐
│  通知所有订阅者                                              │
│  - UI 更新技能列表                                           │
│  - Agent 获取最新技能信息                                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 7. 关键特性总结

| 特性 | 实现方式 | 位置 |
|------|---------|------|
| **技能加载** | 使用 `@mariozechner/pi-coding-agent` | `src/agent/skills/loader.ts` |
| **Prompt 格式化** | `formatSkillsForPrompt()` | `src/agent/skills/formatter.ts` |
| **依赖管理** | 支持 8 种包管理器 | `src/agent/skills/installer.ts` |
| **热重载** | `chokidar` 文件监听 | `src/agent/skills/hot-reload.ts` |
| **系统集成** | System Prompt 注入 | `src/agent/core/system-prompt.ts` |
| **统一管理** | Facade 模式 | `src/agent/skills/skills-manager.ts` |

---

## 8. 双系统设计

```
┌─────────────────────────────────────────────────────────────┐
│  旧系统 (向后兼容)              新系统 (推荐)                │
│                                                              │
│  ┌──────────────────┐         ┌──────────────────┐         │
│  │  SkillRegistry   │         │  SkillsManager   │         │
│  │                  │         │                  │         │
│  │  - 直接执行技能  │         │  - System Prompt │         │
│  │  - 配置触发      │         │  - LLM 自主判断  │         │
│  │  - 基于规则      │         │  - 文档驱动      │         │
│  └──────────────────┘         └──────────────────┘         │
│                                                              │
│  ⚠️  逐渐废弃              ✅  推荐使用                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 9. 核心设计亮点

1. **Facade 模式**: `SkillsManager` 提供统一接口，隐藏复杂实现
2. **文档驱动**: SKILL.md 文件即技能，易于维护和扩展
3. **依赖自管理**: 技能自带安装规范，自动处理依赖
4. **热重载支持**: 开发时修改技能无需重启
5. **灵活集成**: 通过 System Prompt 无缝集成到 Agent
6. **向后兼容**: 保留旧系统，平滑迁移

---

## 10. 关键文件清单

### 核心模块

- `src/agent/skills/index.ts` - Skills 系统统一导出
- `src/agent/skills/skills-manager.ts` - 主要管理类
- `src/agent/skills/types.ts` - 类型定义
- `src/agent/skills/loader.ts` - 技能加载器
- `src/agent/skills/formatter.ts` - 技能格式化器
- `src/agent/skills/installer.ts` - 依赖安装器
- `src/agent/skills/hot-reload.ts` - 热重载模块

### CLI 和接口

- `src/cli/commands/skills.ts` - CLI 命令处理
- `src/agent/core/orchestrator.ts` - 技能编排器
- `src/agent/core/agent.ts` - Agent 集成
- `src/agent/core/system-prompt.ts` - System Prompt 集成

### UI 组件

- `ui/src/ui/components/krebs-skills-panel.ts` - Skills 面板
- `ui/src/ui/components/krebs-skills-list.ts` - Skills 列表

### 技能文件示例

- `skills/bundled/github/SKILL.md` - GitHub 技能
- `skills/bundled/filesystem/SKILL.md` - 文件系统技能
- `skills/bundled/web-search/SKILL.md` - 网络搜索技能

---

## 总结

这个 Skills 系统设计非常优雅，将复杂的技能管理抽象为简单的 Markdown 文件，同时通过 System Prompt 实现了与 AI 的自然集成。整个系统具有高度的可扩展性和可维护性，是一个优秀的设计参考。
