# 工具注册与系统联动流程

> 一图看懂：定义工具后如何让系统知道并调用

```
┌─────────────────────────────────────────────────────────────────────┐
│                        步骤 1: 定义工具                              │
│                     src/agent/tools/my-tool.ts                       │
├─────────────────────────────────────────────────────────────────────┤
│  export const myTool: Tool = {                                      │
│    name: "my_tool",                                                 │
│    description: "工具描述",                                         │
│    inputSchema: { ... },                                            │
│    async execute(params) {                                          │
│      // 工具执行逻辑                                                 │
│      return { success: true, data: ... };                           │
│    }                                                                │
│  }                                                                  │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────┐
│                        步骤 2: 注册工具                              │
│                    src/agent/tools/builtin.ts                       │
├─────────────────────────────────────────────────────────────────────┤
│  import { myTool } from "./my-tool.js";  // ← 导入                  │
│                                                                     │
│  export function getBuiltinTools(): Tool[] {                        │
│    return [                                                         │
│      bashTool,                                                      │
│      readTool,                                                      │
│      myTool,  // ← 添加到数组                                       │
│    ];                                                               │
│  }                                                                  │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────┐
│                        步骤 3: 编译项目                              │
├─────────────────────────────────────────────────────────────────────┤
│  $ npm run build                                                    │
│                                                                     │
│  输出:                                                              │
│  ✅ tsc - 编译 TypeScript → JavaScript                              │
│  ✅ tsc-alias - 处理路径别名                                        │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    步骤 4: Agent 自动加载                            │
├─────────────────────────────────────────────────────────────────────┤
│  const agent = new Agent(config, {                                  │
│    tools: getBuiltinTools()  // ← 自动包含 myTool                   │
│  });                                                                │
│                                                                     │
│  // agent.deps.tools 现在包含 myTool                                │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    步骤 5: 转换为平台格式                            │
├─────────────────────────────────────────────────────────────────────┤
│  const adaptedTools = adaptToolsForDeepSeek(                        │
│    agent.deps.tools                                                 │
│  );                                                                 │
│                                                                     │
│  // myTool 被转换为:                                                │
│  {                                                                  │
│    type: "function",                                                │
│    function: {                                                      │
│      name: "my_tool",                                               │
│      description: "工具描述",                                        │
│      parameters: { ... }                                            │
│    }                                                                │
│  }                                                                  │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    步骤 6: 发送给 LLM                                │
├─────────────────────────────────────────────────────────────────────┤
│  await deepseek.chat.completions.create({                          │
│    model: "deepseek-chat",                                          │
│    messages: [...],                                                 │
│    tools: adaptedTools  // ← 包含 myTool 的声明                     │
│  });                                                                │
│                                                                     │
│  // LLM 现在知道有 my_tool 工具可用                                  │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    步骤 7: LLM 决定调用                              │
├─────────────────────────────────────────────────────────────────────┤
│  用户: "帮我使用 my_tool"                                           │
│    ↓                                                                │
│  LLM 分析: 用户需要 my_tool                                         │
│    ↓                                                                │
│  LLM 返回: {                                                        │
│    tool_calls: [{                                                   │
│      name: "my_tool",                                               │
│      arguments: { param1: "value1" }                                │
│    }]                                                               │
│  }                                                                  │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    步骤 8: 执行工具                                  │
├─────────────────────────────────────────────────────────────────────┤
│  // Agent 查找工具                                                   │
│  const tool = agent.deps.tools.find(                                │
│    t => t.name === "my_tool"                                        │
│  );                                                                 │
│    ↓                                                                │
│  // 执行工具                                                         │
│  const result = await tool.execute({                                │
│    param1: "value1"                                                 │
│  });                                                                │
│    ↓                                                                │
│  // 返回结果给 LLM                                                  │
│  result = { success: true, data: { ... } }                          │
└─────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    步骤 9: LLM 生成回复                              │
├─────────────────────────────────────────────────────────────────────┤
│  Agent: "工具执行成功，结果是..."                                    │
└─────────────────────────────────────────────────────────────────────┘
```

## 核心要点

### ✅ 必须做的（3步）

1. **定义工具** - 创建 `src/agent/tools/my-tool.ts`
2. **注册工具** - 添加到 `builtin.ts` 的 `getBuiltinTools()` 返回数组
3. **编译项目** - 运行 `npm run build`

### 🔑 关键机制

```
getBuiltinTools() → Agent.deps.tools → Platform Adapter → LLM API → Tool Execution
```

### 📝 示例：完整的工具定义

```typescript
// src/agent/tools/greet.ts
import type { Tool } from "./types.js";

export const greetTool: Tool = {
  name: "greet",
  description: "向用户发送问候",
  inputSchema: {
    type: "object",
    properties: {
      name: { type: "string", description: "名字" }
    },
    required: ["name"]
  },
  async execute(params) {
    return {
      success: true,
      data: { message: `你好，${params.name}！` }
    };
  }
};
```

```typescript
// src/agent/tools/builtin.ts
import { greetTool } from "./greet.js";

export function getBuiltinTools(): Tool[] {
  return [
    bashTool,
    readTool,
    greetTool,  // ← 添加这里
  ];
}
```

```bash
# 编译
$ npm run build
```

**完成！** 系统现在知道并可以调用 `greet` 工具了。

---

**更新时间**: 2026-02-05
**版本**: 1.0
