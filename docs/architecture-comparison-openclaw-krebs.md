# openclaw-cn-ds vs Krebs 架构深度对比分析

> **分析时间**: 2026-02-19
> **对比目的**: 找出 Krebs 可以改进的方向
> **分析方法**: 架构层面对比、设计模式对比、功能特性对比

---

## 📊 整体架构对比

### openclaw-cn-ds 架构

```
openclaw-cn-ds/
├── src/
│   ├── agents/                    # Agent 核心（300+ 文件）
│   │   ├── system-prompt.ts        # System Prompt 构建器
│   │   ├── subagent-registry.ts    # Subagent 注册表（核心）
│   │   ├── subagent-announce.ts    # Subagent 通知机制
│   │   ├── pi-embedded-runner/    # 嵌入式 Agent 运行器
│   │   ├── pi-embedded-subscribe/  # 流式订阅处理
│   │   ├── skills/                 # 技能系统
│   │   ├── sandbox/                # 沙盒环境
│   │   ├── tools/                  # 工具定义
│   │   └── ... (298 个文件)
│   │
│   ├── config/                    # 配置系统（70+ 文件）
│   │   ├── config.agent-defaults.ts
│   │   ├── config.agent-runtime.ts
│   │   ├── config.agents.ts
│   │   ├── zod-schema.agents.ts
│   │   └── ...
│   │
│   ├── infra/                    # 基础设施
│   │   ├── agent-events.ts        # Agent 事件系统
│   │   ├── heartbeat-runner.ts    # 心跳检测
│   │   └── ...
│   │
│   ├── scheduler/                # 调度系统
│   │   └── lanes.ts
│   │
│   ├── skills/                   # 技能系统
│   │   ├── skills-install.ts
│   │   └── skills-status.ts
│   │
│   └── ... (大量模块)
```

**特点**：
- 🏢 **企业级规模**: 300+ 个文件在 agents 目录
- 🔧 **功能完整**: Subagent、Sandbox、Skills、Events
- 📦 **高度模块化**: 每个功能独立文件
- 🎯 **生产就绪**: 完善的错误处理、持久化、监控

---

### Krebs 当前架构

```
Krebs/
├── src/
│   ├── agent/                    # Agent 核心（精简）
│   │   ├── core/
│   │   │   ├── agent.ts          # Agent 主类（21KB）
│   │   │   ├── manager.ts        # Agent 管理器
│   │   │   ├── orchestrator.ts    # 技能调度器
│   │   │   ├── system-prompt.ts  # System Prompt（15KB）
│   │   │   └── context-loader.ts # 上下文加载器
│   │   └── skills/               # 技能系统
│   │
│   ├── gateway/                  # 网关层
│   │   ├── service/              # 服务接口
│   │   └── server/               # HTTP/WebSocket
│   │
│   ├── provider/                 # AI 模型抽象
│   ├── storage/                  # 存储层
│   ├── scheduler/                # 调度系统
│   └── types/                    # 类型定义
```

**特点**：
- 🎯 **简洁清晰**: 核心功能集中，易于理解
- 🧩 **模块化设计**: 清晰的分层架构
- 📝 **文档完善**: production.md、架构文档
- 🚀 **快速迭代**: 轻量级，易于修改

---

## 🔍 关键差异分析

### 1. Subagent 系统

#### openclaw-cn-ds 的 Subagent 系统

```
┌────────────────────────────────────────────────────────────────────────────┐
│                        Subagent 系统 (openclaw-cn-ds)                       │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ┌─────────────────┐        spawn_subagent tool         ┌────────────────┐ │
│  │  Main Agent     │  ──────────────────────────────────►│  Subagent      │ │
│  │                 │                                     │  Session       │ │
│  │  - 用户交互      │  任务: "分析这个模块"               │                │ │
│  │  - 任务调度      │                                     │  - 独立执行     │ │
│  │  - 结果汇总      │  ◄────────────────────────────────  │  - 返回结果     │ │
│  └─────────────────┘        notify / callback            └────────────────┘ │
│                                                                            │
│  核心组件:                                                                  │
│  • subagent-registry.ts      - Subagent 注册表                              │
│  • subagent-announce.ts      - Subagent 通知机制                            │
│  • clawdbot-tools.subagents.* - Subagent 工具                              │
│                                                                            │
│  特性:                                                                      │
│  ✅ 隔离会话执行 - Subagent 在独立会话中运行                             │
│  ✅ 结果通知机制 - steer/followup/collect/silent 模式                      │
│  ✅ 持久化存储 - Subagent 状态保存到磁盘                                 │
│  ✅ 生命周期管理 - 启动、监控、清理、归档                                 │
│  ✅ 配置驱动 - 全局默认 + agent 级别覆盖                                  │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

**实现细节**：

```typescript
// Subagent 注册记录
export type SubagentRunRecord = {
  runId: string;
  childSessionKey: string;
  requesterSessionKey: string;
  requesterDisplayKey: string;
  task: string;
  cleanup: "delete" | "keep";
  label?: string;
  createdAt: number;
  startedAt?: number;
  endedAt?: number;
  outcome?: SubagentRunOutcome;
  archiveAtMs?: number;
  cleanupCompletedAt?: number;
};

// 通知模式
export type SubagentNotifyMode =
  | "steer"     // 主导：回到主会话
  | "followup"  // 追问：发送追问
  | "collect"   // 收集：静默收集
  | "silent";   // 静默：不通知
```

#### Krebs 的 Subagent 状态

```
┌────────────────────────────────────────────────────────────────────────────┐
│                        Krebs Subagent 状态                                  │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  当前状态: ❌ 未实现                                                       │
│                                                                            │
│  规划状态: ✅ 已在 production.md 中规划                                    │
│                                                                            │
│  Phase 4 规划:                                                             │
│  • 支持生成后台子代理运行                                                 │
│  • 隔离会话执行复杂任务                                                   │
│  • 结果通知机制（steer、followup、collect、silent模式）                    │
│  • 生命周期管理（注册、执行、通知、清理）                                 │
│  • 持久化存储和恢复机制                                                   │
│  • 配置驱动（全局默认 + agent级别覆盖）                                  │
│  • 作为标准工具集成（spawn_subagent工具）                                │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

**建议**：优先实现 Subagent 系统，这是 openclaw-cn-ds 的核心特性之一。

---

### 2. Agent 事件系统

#### openclaw-cn-ds 的事件系统

```
┌────────────────────────────────────────────────────────────────────────────┐
│                    Agent Events 系统 (openclaw-cn-ds)                       │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  事件流:                                                                    │
│  ┌──────────────┐     onAgentEvent      ┌──────────────┐               │
│  │ Agent Action  │  ────────────────────►│ Event Handler │               │
│  │               │                       │               │               │
│  │ • 工具调用     │                       │ • 保存日志     │               │
│  │ • Subagent    │                       │ • 发送通知     │               │
│  │ • 错误发生     │                       │ • 触发钩子     │               │
│  └──────────────┘                       └──────────────┘               │
│                                                     │                   │
│                                                     ▼                   │
│  ┌────────────────────────────────────────────────────────────────┐   │
│  │ 事件类型                                                          │   │
│  │ • tool_start    - 工具开始执行                                    │   │
│  │ • tool_end      - 工具执行结束                                    │   │
│  │ • tool_error    - 工具执行错误                                    │   │
│  │ • subagent_spawn - 子代理生成                                     │   │
│  │ • subagent_done  - 子代理完成                                     │   │
│  └────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│  核心文件:                                                                  │
│  • infra/agent-events.ts      - 事件发射器                               │
│  • infra/agent-events.test.ts - 事件测试                                 │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

#### Krebs 的事件系统

```
┌────────────────────────────────────────────────────────────────────────────┐
│                        Krebs 事件系统状态                                   │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  当前状态: ⚠️ 部分实现                                                   │
│                                                                            │
│  已有事件:                                                                  │
│  • scheduler/lanes.ts - Lane 调度事件                                     │
│  • storage/memory/ - Memory 更新事件                                      │
│                                                                            │
│  缺失事件:                                                                  │
│  ❌ Agent 生命周期事件                                                     │
│  ❌ 工具调用事件                                                           │
│  ❌ Subagent 事件                                                         │
│  ❌ 错误追踪事件                                                          │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

**建议**：实现 Agent 事件系统，用于：
- 📊 日志和监控
- 🔔 通知和回调
- 🐛 调试和追踪
- 📈 性能分析

---

### 3. 配置系统

#### openclaw-cn-ds 的配置系统

```
┌────────────────────────────────────────────────────────────────────────────┐
│                    配置系统 (openclaw-cn-ds)                                │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  配置文件结构:                                                              │
│  ├── config.agent-defaults.ts    - Agent 默认配置                         │
│  ├── config.agent-runtime.ts     - Agent 运行时配置                       │
│  ├── config.agents.ts             - Agents 配置                            │
│  ├── zod-schema.agents.ts         - Zod 验证 Schema                       │
│  └── ... (70+ 配置文件)                                                     │
│                                                                            │
│  配置特性:                                                                  │
│  ✅ 多层级配置 - 全局默认 → agent 配置 → 运行时覆盖                      │
│  ✅ Zod 验证 - 类型安全的配置验证                                         │
│  ✅ 动态重载 - 配置变更自动生效                                           │
│  ✅ 环境变量支持 - .env 文件集成                                          │
│  ✅ 配置继承 - agent 继承全局默认                                         │
│                                                                            │
│  配置示例:                                                                  │
│  interface AgentConfig {                                                  │
│    defaults: {                                                            │
│      model: string;           // 默认模型                                  │
│      systemPromptMode: "full" | "minimal" | "none";                      │
│      userTimezone: string;     // 用户时区                                │
│      reasoningLevel: "off" | "on" | "stream";                          │
│      verboseLevel: "off" | "terse" | "normal" | "verbose";             │
│    }                                                                      │
│    agents: {                                                              │
│      [agentId: string]: {                                                │
│        model?: string;         // agent 级别模型覆盖                       │
│        systemPromptMode?: ...;  // agent 级别模式覆盖                    │
│        sandbox?: {             // 沙盒配置                                │
│          enabled: boolean;                                               │
│          workspaceDir?: string;                                         │
│        }                                                                  │
│        subagent?: {           // 子代理配置                               │
│          allowlist?: string[];  // 允许生成的 subagent                   │
│          model?: string;       // subagent 模型                          │
│        }                                                                  │
│      }                                                                    │
│    }                                                                      │
│  }                                                                        │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

#### Krebs 的配置系统

```
┌────────────────────────────────────────────────────────────────────────────┐
│                       Krebs 配置系统状态                                    │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  当前状态: ⚠️ 基础实现                                                   │
│                                                                            │
│  配置文件:                                                                  │
│  ├── src/shared/config.ts         - 基础配置管理                           │
│  ├── src/agent/core/config.ts    - Agent 配置                             │
│  └── .env 文件                    - 环境变量                                │
│                                                                            │
│  缺失特性:                                                                  │
│  ❌ 多层级配置                                                             │
│  ❌ Zod 验证                                                               │
│  ❌ 动态重载                                                               │
│  ❌ 配置继承                                                               │
│  ❌ agent 级别覆盖                                                        │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

**建议**：增强配置系统，支持：
- 🎛️ 多层级配置（全局 → agent → 运行时）
- ✅ Zod 验证（类型安全）
- 🔄 动态重载（无需重启）
- 🧬 配置继承（agent 继承全局默认）

---

### 4. 技能系统

#### openclaw-cn-ds 的技能系统

```
┌────────────────────────────────────────────────────────────────────────────┐
│                      Skills 系统 (openclaw-cn-ds)                             │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  技能加载位置:                                                              │
│  1. Bundled Skills   - 内置技能（@mariozechner/pi-coding-agent 包）         │
│  2. Managed Skills   - 全局管理技能（skills/ 目录）                        │
│  3. Workspace Skills  - 项目级技能（workspace/.skills/）                    │
│                                                                            │
│  技能格式 (SKILL.md):                                                      │
│  ┌──────────────────────────────────────────────────────────────────────┐ │
│  │  # Skill Name                                                           │ │
│  │  Description: What this skill does                                     │ │
│  │                                                                         │ │
│  │  ## Usage                                                               │ │
│  │  - Step 1: ...                                                          │ │
│  │  - Step 2: ...                                                          │ │
│  │                                                                         │ │
│  │  ## Examples                                                            │ │
│  │  - Example 1: ...                                                       │ │
│  │                                                                         │ │
│  │  ## Notes                                                               │ │
│  │  - Additional notes...                                                 │ │
│  └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│  核心功能:                                                                  │
│  ✅ 技能热加载 - 文件变化自动重新加载                                       │
│  ✅ Frontmatter 解析 - 从 SKILL.md 提取元数据                              │
│  ✅ 技能注入 - 动态注入到 System Prompt                                     │
│  ✅ 技能查询 - agent 可以查询可用技能                                       │
│  ✅ 技能安装 - 自动安装技能依赖                                            │
│                                                                            │
│  核心文件:                                                                  │
│  • agents/skills.ts             - 技能加载器                                │
│  • agents/skills-install.ts     - 技能安装器                               │
│  • agents/skills-status.ts      - 技能状态查询                             │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

#### Krebs 的技能系统

```
┌────────────────────────────────────────────────────────────────────────────┐
│                        Krebs Skills 系统状态                                  │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  当前状态: ⚠️ 部分实现                                                   │
│                                                                            │
│  已实现:                                                                    │
│  ✅ src/agent/skills/            - 技能基类和注册表                        │
│  ✅ 技能热加载 - chokidar 监听文件变化                                     │
│  ✅ Frontmatter 解析 - 支持 SKILL.md 格式                                 │
│  ✅ 技能注入 - orchestrator.ts 注入到 Prompt                               │
│                                                                            │
│  缺失特性:                                                                  │
│  ❌ Bundled Skills - 不支持 pi-coding-agent 包                            │
│  ❌ Managed Skills - 没有全局 skills/ 目录                               │
│  ❌ Workspace Skills - 没有 workspace/.skills/ 支持                      │
│  ❌ 技能安装 - skills-install.ts                                           │
│  ❌ 技能状态查询 - skills-status.ts                                        │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

**建议**：增强技能系统，支持：
- 📦 Bundled Skills（pi-coding-agent 集成）
- 📁 Managed Skills（全局 skills/ 目录）
- 🏠 Workspace Skills（项目级技能）
- 🔧 技能安装和状态查询

---

### 5. 沙盒系统

#### openclaw-cn-ds 的沙盒系统

```
┌────────────────────────────────────────────────────────────────────────────┐
│                      Sandbox 系统 (openclaw-cn-ds)                            │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  沙盒类型:                                                                  │
│  • Docker 沙盒 - 容器化隔离环境                                           │
│  • VM 沙盒 - 虚拟机隔离                                                   │
│  • chroot - 目录隔离                                                     │
│                                                                            │
│  沙盒特性:                                                                  │
│  ✅ 工作区隔离 - agent 只能访问指定目录                                   │
│  ✅ 资源限制 - CPU、内存、磁盘限制                                        │
│  ✅ 浏览器控制 - 集成浏览器控制                                           │
│  ✅ 权限提升 - elevated exec 支持 approvals                               │
│  ✅ 环境变量 - 沙盒环境变量配置                                           │
│                                                                            │
│  System Prompt 集成:                                                       │
│  ┌──────────────────────────────────────────────────────────────────────┐ │
│  │  ## Sandbox                                                            │ │
│  │  You are running in a sandboxed runtime (Docker).                    │ │
│  │  - Sandbox workspace: /workspace                                     │ │
│  │  - Agent workspace access: rw                                          │ │
│  │  - Sandbox browser control URL: ws://browser:3000                    │ │
│  │  - Host browser control: blocked                                      │ │
│  │  - Elevated exec is available for this session.                       │ │
│  └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

#### Krebs 的沙盒系统

```
┌────────────────────────────────────────────────────────────────────────────┐
│                        Krebs Sandbox 系统状态                                  │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  当前状态: ❌ 未实现                                                       │
│                                                                            │
│  已有基础:                                                                  │
│  ✅ SandboxInfo 接口定义                                                 │
│  ✅ buildSandboxSection 函数                                               │
│                                                                            │
│  缺失特性:                                                                  │
│  ❌ 实际沙盒环境集成                                                       │
│  ❌ Docker 容器支持                                                        │
│  ❌ 工作区隔离                                                             │
│  ❌ 资源限制                                                               │
│  ❌ 浏览器控制                                                             │
│  ❌ elevated exec                                                          │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

**建议**：沙盒系统是高级特性，可以暂缓实现。

---

## 🎯 核心改进建议

基于以上对比，我建议按以下优先级改进 Krebs：

### 优先级 P0（核心特性）

#### 1. 实现 Subagent 系统 ⭐⭐⭐⭐⭐

**原因**：openclaw-cn-ds 的核心特性，极大提升 Agent 能力

**实现方案**：
```typescript
// 1. 创建 SubagentRegistry
export class SubagentRegistry {
  private runs = new Map<string, SubagentRunRecord>();

  spawn(config: SubagentSpawnConfig): string {
    const runId = generateId();
    // 创建子会话
    // 保存记录
    return runId;
  }

  complete(runId: string, outcome: SubagentRunOutcome) {
    // 标记完成
    // 触发通知
  }
}

// 2. 创建 spawn_subagent 工具
const spawnSubagentTool: Tool = {
  name: "spawn_subagent",
  description: "Spawn a subagent to handle a task",
  parameters: {
    task: "string",
    agentId: "string",
    notifyMode: "steer|followup|collect|silent",
  },
  execute: async (args) => {
    // 调用 SubagentRegistry
  }
};

// 3. 添加到 Agent 的工具列表
agent.tools.push(spawnSubagentTool);
```

**收益**：
- ✅ 支持复杂任务分解
- ✅ 并行处理多个任务
- ✅ 隔离执行环境
- ✅ 结果通知机制

---

#### 2. 实现 Agent 事件系统 ⭐⭐⭐⭐

**原因**：监控、调试、日志的基础设施

**实现方案**：
```typescript
// 1. 定义事件类型
export type AgentEvent =
  | { type: "tool_start"; toolName: string; timestamp: number }
  | { type: "tool_end"; toolName: string; duration: number; success: boolean }
  | { type: "subagent_spawn"; runId: string; task: string }
  | { type: "error"; error: Error; context: any };

// 2. 创建事件发射器
export class AgentEventEmitter {
  private listeners = new Map<string, Set<Function>>();

  on(event: string, handler: Function) {
    if (!this.listeners.has(event)) {
      this.listeners.set(event, new Set());
    }
    this.listeners.get(event)!.add(handler);
  }

  emit(event: AgentEvent) {
    const handlers = this.listeners.get(event.type);
    if (handlers) {
      for (const handler of handlers) {
        try {
          handler(event);
        } catch (error) {
          console.error(`Event handler error for ${event.type}:`, error);
        }
      }
    }
  }
}

// 3. 集成到 Agent
export class Agent {
  private eventEmitter = new AgentEventEmitter();

  async processWithTools(...) {
    this.eventEmitter.emit({
      type: "tool_start",
      toolName: tool.name,
      timestamp: Date.now()
    });

    try {
      const result = await tool.execute(...);
      this.eventEmitter.emit({
        type: "tool_end",
        toolName: tool.name,
        duration: Date.now() - startTime,
        success: true
      });
    } catch (error) {
      this.eventEmitter.emit({
        type: "tool_end",
        toolName: tool.name,
        duration: Date.now() - startTime,
        success: false
      });
    }
  }
}
```

**收益**：
- ✅ 完整的日志追踪
- ✅ 性能监控
- ✅ 错误调试
- ✅ 事件钩子（可用于通知）

---

### 优先级 P1（增强特性）

#### 3. 增强配置系统 ⭐⭐⭐

**实现方案**：
```typescript
// 1. 多层级配置
interface Config {
  defaults: {
    model: string;
    systemPromptMode: "full" | "minimal" | "none";
    reasoningLevel: "off" | "on" | "stream";
  };
  agents: {
    [agentId: string]: {
      model?: string;
      systemPromptMode?: "full" | "minimal" | "none";
      subagent?: {
        allowlist?: string[];
      };
    };
  };
}

// 2. Zod 验证
import { z } from "zod";

const AgentConfigSchema = z.object({
  model: z.string().optional(),
  systemPromptMode: z.enum(["full", "minimal", "none"]).optional(),
  // ...
});

// 3. 配置加载器
class ConfigLoader {
  private config: Config;

  load() {
    // 加载 .env
    // 加载 config.yaml
    // 验证配置
    // 合并多层级配置
  }

  getAgentConfig(agentId: string) {
    const defaults = this.config.defaults;
    const agentConfig = this.config.agents[agentId];
    return { ...defaults, ...agentConfig };
  }
}
```

**收益**：
- ✅ 类型安全的配置
- ✅ 多 agent 支持
- ✅ 配置继承和覆盖
- ✅ 运行时动态配置

---

#### 4. 增强技能系统 ⭐⭐⭐

**实现方案**：
```typescript
// 1. 多位置技能加载
class SkillLoader {
  loadBundledSkills(): Skill[] {
    // 从 @mariozechner/pi-coding-agent 加载
  }

  loadManagedSkills(): Skill[] {
    // 从 skills/ 目录加载
  }

  loadWorkspaceSkills(): Skill[] {
    // 从 workspace/.skills/ 加载
  }

  loadAllSkills(): Skill[] {
    return [
      ...this.loadBundledSkills(),
      ...this.loadManagedSkills(),
      ...this.loadWorkspaceSkills(),
    ];
  }
}

// 2. 技能状态查询
class SkillStatus {
  listSkills(): SkillInfo[] {
    return this.skills.map(skill => ({
      name: skill.name,
      description: skill.description,
      loaded: true,
      version: skill.version,
    }));
  }

  installSkill(skillName: string) {
    // 安装技能依赖
  }
}

// 3. 技能工具
const skillsListTool: Tool = {
  name: "skills_list",
  description: "List all available skills",
  execute: async () => {
    return skillStatus.listSkills();
  }
};
```

**收益**：
- ✅ 更丰富的技能生态
- ✅ 项目级技能定制
- ✅ 技能热加载和安装

---

### 优先级 P2（可选特性）

#### 5. 沙盒系统 ⭐⭐

**原因**：安全隔离，但实现复杂度高

**建议**：暂缓，先实现 P0 和 P1

---

## 📊 改进优先级总结

```
┌────────────────────────────────────────────────────────────────────────────┐
│                          改进优先级对比                                      │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  P0 - 核心特性 (必须实现)                                                  │
│  ┌──────────────────────────────────────────────────────────────────────┐ │
│  │ 1. Subagent 系统 ⭐⭐⭐⭐⭐                                           │ │
│  │    - 复杂任务分解                                                      │ │
│  │    - 并行处理能力                                                      │ │
│  │    - 隔离执行环境                                                      │ │
│  │    - 参考实现: subagent-registry.ts                                   │ │
│  │                                                                      │ │
│  │ 2. Agent 事件系统 ⭐⭐⭐⭐                                            │ │
│  │    - 监控和日志                                                        │ │
│  │    - 性能追踪                                                          │ │
│  │    - 错误调试                                                          │ │
│  │    - 参考实现: infra/agent-events.ts                                   │ │
│  └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│  P1 - 增强特性 (强烈建议)                                                  │
│  ┌──────────────────────────────────────────────────────────────────────┐ │
│  │ 3. 增强配置系统 ⭐⭐⭐                                                 │ │
│  │    - 多层级配置                                                        │ │
│  │    - Zod 验证                                                          │ │
│  │    - 动态重载                                                          │ │
│  │    - 参考实现: config/agents.ts                                         │ │
│  │                                                                      │ │
│  │ 4. 增强技能系统 ⭐⭐⭐                                                 │ │
│  │    - Bundled Skills                                                   │ │
│  │    - Managed/Workspace Skills                                         │ │
│  │    - 技能安装和状态查询                                                │ │
│  │    - 参考实现: agents/skills.ts                                        │ │
│  └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│  P2 - 可选特性 (暂缓)                                                      │
│  ┌──────────────────────────────────────────────────────────────────────┐ │
│  │ 5. 沙盒系统 ⭐⭐                                                       │ │
│  │    - Docker 隔离                                                       │ │
│  │    - 资源限制                                                          │ │
│  │    - 参考实现: agents/sandbox/                                       │ │
│  │                                                                      │ │
│  │ 6. 心跳检测 ⭐                                                         │ │
│  │    - 定期健康检查                                                      │ │
│  │    - 参考实现: infra/heartbeat-runner.ts                              │ │
│  └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

---

## 🚀 实施路线图

### 第一阶段：Subagent 系统（2-3周）

```
Week 1: 核心 Subagent 框架
├── SubagentRegistry 类
├── SubagentRunRecord 类型
├── spawn_subagent 工具
└── 基础测试

Week 2: 通知和清理机制
├── SubagentAnnounce 类
├── 通知模式（steer/followup/collect/silent）
├── 生命周期管理
└── 集成测试

Week 3: 持久化和集成
├── 磁盘存储
├── Agent 集成
└── 文档和示例
```

### 第二阶段：事件系统（1-2周）

```
Week 1: 事件发射器
├── AgentEventEmitter 类
├── 事件类型定义
├── Agent 集成
└── 单元测试

Week 2: 事件监听和日志
├── 日志适配器
├── 性能监控
├── 错误追踪
└── 集成测试
```

### 第三阶段：配置和技能增强（2-3周）

```
Week 1-2: 配置系统
├── 多层级配置
├── Zod 验证
├── 动态重载
└── 文档

Week 2-3: 技能系统
├── Bundled Skills
├── Managed/Workspace Skills
├── 技能安装和状态查询
└── 文档和示例
```

---

## ✨ 总结

### Krebs 的优势

- ✅ **简洁清晰** - 易于理解和维护
- ✅ **快速迭代** - 轻量级，易于修改
- ✅ **文档完善** - production.md、架构文档
- ✅ **基础扎实** - 已有 System Prompt、Skills、Memory 等核心功能

### openclaw-cn-ds 的优势

- ✅ **功能完整** - Subagent、Sandbox、Events、Skills
- ✅ **生产就绪** - 完善的错误处理、持久化、监控
- ✅ **高度模块化** - 每个功能独立文件
- ✅ **配置灵活** - 多层级配置、Zod 验证、动态重载

### 最佳策略

**保持 Krebs 的简洁性，逐步引入 openclaw-cn-ds 的核心特性**：

1. ✅ 已完成：System Prompt 增强
2. 🎯 下一步：Subagent 系统（P0）
3. 🎯 然后：Agent 事件系统（P0）
4. 🎯 最后：配置和技能增强（P1）

**目标**：在保持简洁的同时，获得企业级的功能完整性！

---

**文档维护**: 本文档应随架构演进同步更新。

**最后更新**: 2026-02-19
